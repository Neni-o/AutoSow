plugins {
    id "java"
    id "net.neoforged.moddev" version "2.0.107"
}

neoForge {
    version = "21.8.31"
    runs {
        client { client() }
        server { server() }
    }
    mods { autosow { sourceSet sourceSets.main } }
}
